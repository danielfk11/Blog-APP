<% if @post %>
  <h1><%= @post.title %></h1>
  <p><%= @post.content %></p>

  <h2>Comentários</h2>
  <% @post.comments.each do |comment| %>
    <div>
      <p><%= comment.content %></p>
    </div>
  <% end %>
<% end %>
