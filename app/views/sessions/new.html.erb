<!DOCTYPE html>
<html>
<head>
  <title style="font-family: 'Helvetica Neue', Arial, sans-serif;"><%= t('sessions.new.title') %></title>
  <style>
    body {
      font-family: 'Helvetica Neue', Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f4f4f4;
    }
    h1 {
      background-color: #1da1f2;
      color: white;
      padding: 20px;
      margin: 0;
    }
    label {
      display: block;
      margin-top: 10px;
      color: #333;
    }
    input[type="email"],
    input[type="password"],
    input[type="submit"] {
      width: 100%;
      padding: 10px;
      margin-top: 5px;
      border: 1px solid #ccc;
      border-radius: 4px;
    }
    input[type="submit"] {
      background-color: #1da1f2;
      color: white;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }
    input[type="submit"]:hover {
      background-color: #0d8bf2;
    }
    p {
      text-align: center;
      margin-top: 20px;
    }
    a {
      color: #1da1f2;
      text-decoration: none;
    }
    a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

<h1><%= t('sessions.new.title') %></h1>

<%= form_for(@user, as: :user, url: session_path(:user)) do |form| %>
  <%= form.label :email, t('sessions.new.email_label') %>
  <%= form.email_field :email, autofocus: true %>

  <%= form.label :password, t('sessions.new.password_label') %>
  <%= form.password_field :password %>

  <%= form.submit t('sessions.new.login_button') %>
<% end %>

<p>
<%= link_to t('sessions.new.forgot_password_link'), forgot_password_path %>
</p>

</body>
</html>
